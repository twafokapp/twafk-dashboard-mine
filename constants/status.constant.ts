export enum STATUS {
  ONLINE = 1,
  OFFLINE = 0,
}

export enum PAYMENT_METHOD {
  CASH = 1,
  VISA = 2,
}

export enum SHIPMENTS_STATUS {
  FAILURE = 0,
  PENDING = 1,
  RETURNED = 2,
  DELIVERED = 3,
}
